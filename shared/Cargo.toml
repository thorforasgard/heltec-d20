[package]
name = "heltec-d20-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = { version = "0.29", default-features = false }
heapless = "0.8"

[features]
default = ["std"]
std = []
